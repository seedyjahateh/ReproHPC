# Candidate license review

This review describes unpublished candidate 5, SIF SHA-256 `7056aa1d75f0062c3f7383e0d1bc695c65f7c232edebdb5a6307149eca6fb849`. It records what is present and what must be resolved before public binary redistribution. The project license does not replace dependency licenses.

The actual SIF inventory is retained in `evidence/licenses-candidate-5.json`: 12 Python distributions and 178 installed notice files, each with original text and SHA-256. The refined filename filter excludes an unrelated license-plate image-classifier file. `evidence/candidate-5.json` records the installed Ubuntu package versions. `evidence/binary-build-candidate-5.json` records OpenCV build information and the bundled FFmpeg library's own license/configuration strings.

| Component | Finding and retained evidence | Release action |
|---|---|---|
| Newly authored ReproHPC software | Existing repository MIT license and copyright retained. No third-party domain routine was supplied. | Preserve the full license and confirm the final creator metadata. |
| Project documentation, synthetic fixtures and calibration | CC BY 4.0 and CC0 1.0 texts are in `LICENSES/`; synthetic origin is described by generator/metadata. | Confirm the deposit creators and their authority for the dedication. Imported future datasets need separate terms. |
| Python dependencies | Installed notices found for every one of the 12 distributions, including pip/setuptools/wheel vendored packages. NumPy's wheel contains bundled-library notices; OpenCV packaging and OpenCV itself have separate notices. | Include the harvested notices with the archived binary/source manifest. Do not describe the whole SIF as MIT. |
| OpenCV headless binary | Build reports OpenCV 4.12.0, GUI `NONE`, non-free algorithms `NO`, and FFmpeg enabled. These observations concern the actual installed wheel. | Preserve its Apache/MIT and third-party notices, including media and acceleration libraries, even though this pipeline only reads PNG. |
| Bundled FFmpeg | `libavcodec` reports `LGPL version 2.1 or later`. Configuration includes shared libraries, OpenSSL and libvpx; no `--enable-gpl` or `--enable-nonfree` flag is present in that recorded configuration. | Resolve and archive matching source/build configuration and dependency terms before distribution. A license string alone does not complete this review. Use the upstream [FFmpeg redistribution guidance](https://ffmpeg.org/legal.html). |
| Ubuntu runtime packages | Versions and `/usr/share/doc/*/copyright` texts are captured. The image includes GPL/LGPL components such as shell/core utilities in addition to Python. | Prepare corresponding-source retrieval/preservation for the exact redistributed package versions and retain their notices. Current SIF retention is local development evidence, not a completed public source offer. |
| Optionally imported real data | BBBC039v1 is fetched on request by `scripts/fetch_bbbc039.py`, verified against pinned SHA-256 values and converted locally. It is CC0 1.0 per its image set page; no archive or converted image is redistributed by this repository, and the derived dataset is gitignored. See [real microscopy data](real-data.md). | Keep the requested citation with any published result derived from it. Confirm terms per image set before importing another, since this collection assigns licences individually. Redistributing a converted copy inside a release would make it a distributed component requiring its own review. |
| Citation schema | Unmodified CFF 1.2.0 schema, upstream URLs/checksums, full CC BY 4.0 license retained in `tests/vendor/cff-1.2.0/`. | Preserve attribution in the source archive. |
| Lab tools | Nextflow, Slurm and Apptainer are in the separate disposable lab, outside the analysis SIF. Versioned download/build instructions and top-level licenses are documented. | Reassess the artifact inventory if publishing the lab image as a binary asset. |

No incompatible original-routine license was discovered because no original routine was provided. The remaining binary-source preservation and creator/rights items are explicit release work, tracked under A-07/T-13. A public DOI must not be used to imply these items passed. A new candidate requires an inventory matching its own SIF bytes.
