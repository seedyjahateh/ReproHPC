process.executor = 'slurm'
process.queue = params.partition
process.clusterOptions = [params.account ? "--account=${params.account}" : '', params.qos ? "--qos=${params.qos}" : ''].findAll { it }.join(' ')
process {
    withName: ANALYZE_BATCH { maxForks = params.max_inflight }
}
