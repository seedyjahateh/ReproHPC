# ReproHPC implementation tracker

Source of truth: [PRD.md](PRD.md). Checked boxes mean implemented AND verified; code existence alone is insufficient.

## UI refinement - 2026-09-15 (completed 2026-09-16)

User direction: refactor all existing UI surfaces to a premium visual standard. The product's UI is its generated offline report; there is no existing web service or Open OnDemand implementation. Decision D-05: keep the report self-contained and introduce four connected views, backed by actual scientific outputs, without adding a service or changing scientific computation.

- [x] **UI-01 - Cohesive visual design.** Acceptance: overview, image explorer, measurements, and provenance use the same responsive typography, color, spacing, navigation, and component system; desktop and mobile screenshots inspected. Evidence: eight screenshots under `evidence/ui/` at 1440 and 390 pixels wide.
- [x] **UI-02 - Working interactions.** Acceptance: sample search, QC filters, image selection and deep links, complete paginated/sortable image measurements, and artifact downloads work with real data; absent previews and null measurements have explicit states. Evidence: 20/20 browser tests; `check_report_links` resolves all 43 report link targets (every per-sample download) in the candidate-7 run and export; Chrome against the served export received HTTP 200 for all 43, downloaded `objects.csv`, and recorded no errors or off-host requests (`evidence/ui-served-export-candidate-7.json`).
- [x] **UI-03 - Accessibility and offline use.** Acceptance: keyboard navigation, visible focus, labels, reduced motion, no page overflow at tested widths, automated WCAG A/AA checks on all views, zero remote requests, and useful no-JavaScript fallback. Evidence: `evidence/ui-browser-results.json`, 20/20 tests passing, zero failures/skips. Automated axe checks cover all four views at both sizes; this is not a complete accessibility certification.
- [x] **UI-04 - Reproducibility and packaging.** Acceptance: deterministic report generation, safe embedded JSON, capped 24 previews, unchanged full-precision science, assets in wheel and SIF, genuine Nextflow/Apptainer run plus export verification. Evidence: `evidence/report-acceptance-candidate-7/` (real Nextflow 25.10.4 + Apptainer 1.5.3 run, exact golden comparison, verified run and export, byte-identical report in both, baked SIF package equal to the checkout); wheel/sdist contain all three assets; unit tests cover determinism, JSON escaping and the preview cap.
- [x] **UI-05 - Delivery and preview.** Acceptance: browser regression suite and CI steps documented, project checks pass, current UI served at the local preview URL, continuation notes and evidence updated. Evidence: `docs/testing.md`, `.github/workflows/test.yml`; cycle-11 full check and Actionlint below; verified candidate-7 export served at `http://127.0.0.1:8765/report/index.html` from `artifacts/report-candidate-7`. Hosted CI execution remains A-05.

Implementation: report moved from inline HTML in `task.py` to `reporting.py` and packaged HTML/CSS/JS assets. Python task API and Nextflow process interface are preserved. Baseline 141 unit tests plus 10 new report/source-snapshot tests passed; wheel and sdist build, Ruff and Actionlint passed. Scientific CSV values are unchanged. Browser QA found and fixed low-contrast labels and an absolutely positioned table caption escaping its horizontal scrolling container on mobile. The source archive and Docker context now exclude installed browser dependencies/results while retaining test sources and lockfiles. Historical candidate-4/5 packages remain unchanged. Python-only browser fixtures are explicitly test data and do not establish HPC execution.

Completed in cycle 10/11 below. Do not serve the Python-only browser fixtures as HPC execution evidence.

## Initial inspection - 2026-09-12

- Existing: README.md, PRD.md, MIT LICENSE (copyright seedyjahateh). No application, tests, workflow, containers, fixtures, or CI.
- No applicable AGENTS.md found in the repository or ancestor directories.
- Preserve pre-existing README.md modification and untracked PRD.md.
- Windows host has Python 3.12, uv, Docker Desktop Linux engine, and Git. Docker works; no host Java or normal Linux WSL distribution.
- Decision D-01: implement demo-cv-v1 from PRD because no original scientific routine exists. Scientific validation remains synthetic, without domain endorsement.
- Decision D-02: use Docker-hosted Linux development/lab, with real Apptainer and Slurm; never substitute mocked scheduling for acceptance.
- P1 features remain optional under the PRD, tracked separately. No release is complete until all P0 gates pass.

## Requirement checklist

- [x] **G-01 - Preserve the original routine's scientific behavior.** Acceptance: Characterized fixtures, documented algorithm contract, comparison with the original routine, and domain review where available.
- [ ] **G-02 - Enable one-command reproduction after prerequisite installation.** Acceptance: Fresh-directory reproduction using the published release lock on local Linux and Slurm.
- [ ] **G-03 - Demonstrate competent HPC execution.** Acceptance: Real array jobs, bounded resource requests, accounting, cancellation, resume, and measured scaling.
- [ ] **G-04 - Make results traceable, reusable, and citable.** Acceptance: Validated provenance, documented units, licenses, immutable artifacts, and resolving version DOIs.
- [x] **G-05 - Produce maintainable research software.** Acceptance: Small DSL2 workflow, tested Python modules, separated site configuration, and a usable tutorial.
- [ ] **US-01 - As a scientist, I want to reproduce the published example with one command.** Acceptance: `reproduce` retrieves locked artifacts, verifies checksums, runs the workflow, compares results, and exits zero only after all checks pass.
- [x] **US-02 - As a scientist, I want to analyze my own images.** Acceptance: A valid manifest and parameter file produce one summary row and declared artifacts per image. Invalid input fails with the affected sample identified.
- [x] **US-03 - As a scientist, I want to change parameters without modifying code.** Acceptance: A YAML or CLI threshold change appears in resolved configuration and invalidates affected analysis tasks.
- [x] **US-04 - As a scientist, I want to recover interrupted work.** Acceptance: Resume reuses completed intact batches and executes unfinished batches, producing each expected sample exactly once.
- [x] **US-05 - As an RSE, I want to trace a measurement.** Acceptance: A result row resolves to input SHA-256, reference version, parameters, algorithm revision, SIF checksum, and producing task attempt.
- [x] **US-06 - As a sysadmin, I want to control execution through configuration.** Acceptance: Changing site settings changes observed Slurm requests without editing scientific code.
- [x] **US-07 - As a sysadmin, I want failures to stop resource consumption predictably.** Acceptance: A terminal failure produces nonzero status and cancels this run's remaining jobs within the tested cleanup interval.
- [ ] **US-08 - As a hiring manager, I want verifiable evidence.** Acceptance: The release includes tests, raw benchmark measurements, real Slurm traces, limitations, and DOI-linked reproduction artifacts.
- [x] **FR-01 - Validate configuration and prerequisites before analysis submission.** Acceptance: Invalid parameters, unsupported tools, missing SIF, unwritable output, or insufficient estimated storage produce actionable errors and zero analysis jobs.
- [x] **FR-02 - Execute `VALIDATE_DATASET` on the selected executor.** Acceptance: Verify manifest consistency, file sizes, SHA-256 values, image headers, and reference checksums before analysis begins.
- [x] **FR-03 - Plan deterministic batches.** Acceptance: Sort by `sample_id`; assign every sample to exactly one batch; include the final partial batch. Repeated planning produces identical membership.
- [x] **FR-04 - Execute `ANALYZE_BATCH` independently per batch.** Acceptance: Process images sequentially within each task; produce all required artifacts; release per-image buffers after use.
- [x] **FR-05 - Parameterize all scientifically relevant behavior.** Acceptance: Validate types, ranges, and unknown keys; persist resolved values; include consumed parameters in task identity.
- [x] **FR-06 - Aggregate only complete successful analysis.** Acceptance: Verify the exact expected sample-ID set, reject duplicates/missing samples, and produce deterministically ordered summaries.
- [x] **FR-07 - Generate an offline report.** Acceptance: Self-contained HTML contains parameters, counts, QC, capped thumbnails, and links to scientific tables.
- [x] **FR-08 - Publish independent result files.** Acceptance: Copy declared artifacts into a unique run directory; completed results do not depend on symlinks into `work/`. Downstream tasks consume channel outputs, not asynchronously published paths.
- [x] **FR-09 - Finalize provenance after engine completion.** Acceptance: Success is recorded only after output completeness, checksums, and schemas pass. Failed runs retain status and available diagnostics. Candidate-5 finalization writes `status.json` last after virtual-metadata checksum and independent verification; 22 unit regression cases cover corruption and terminal-write failures.
- [x] **FR-10 - Support local and Slurm profiles.** Acceptance: Identical modules, scientific parameters, inputs, and SIF pass the same scientific comparison.
- [x] **FR-11 - Use native Slurm arrays.** Acceptance: Ten analysis batches with array size four produce groups of four, four, and two elements, with preserved job-to-batch mapping.
- [x] **FR-12 - Resume an explicitly selected session.** Acceptance: Completed intact batches are cached; failed, incomplete, or missing-output batches execute again.
- [x] **FR-13 - Support cancellation and cleanup.** Acceptance: Handled interruption requests engine shutdown and targets only this run's jobs. Hard-kill recovery is documented and tested.
- [x] **FR-14 - Export a reproducibility package.** Acceptance: Export contains scientific outputs, resolved parameters, manifests, schemas, checksums, software identities, and immutable artifact retrieval instructions. Actual candidate-4 tutorial export passed roundtrip verification with redactions and archive checksum.
- [x] **NFR-01 - Reproducibility** Acceptance: Pass the exact and tolerant comparisons in Section 14 with individually verifiable artifacts.
- [x] **NFR-02 - Declared scalability** Acceptance: Support manifests of 1-10,000 images within image limits. Execute at least 256 end-to-end; test planning on 10,000. Label planning-only evidence accurately.
- [x] **NFR-03 - Bounded memory** Acceptance: Process one decoded image at a time; stream aggregation; pass maximum-size image and 1,000,000-row aggregation fixtures within allocated memory.
- [ ] **NFR-04 - Runtime efficiency** Acceptance: Measure overhead against the same serial routine and meet the qualified four-worker scaling target.
- [x] **NFR-05 - Portability** Acceptance: Run local and Slurm profiles on Linux x86-64 without changing scientific code. Keep scheduler-specific settings outside Python and workflow modules.
- [x] **NFR-06 - Offline execution** Acceptance: After preparation, execute successfully without outbound network access.
- [x] **NFR-07 - Filesystem integrity** Acceptance: Immutable inputs, isolated outputs, atomic per-file writes, no overwrite of completed runs, and validated path boundaries.
- [x] **NFR-08 - Operability** Acceptance: Every invocation has status, logs, trace, output validation, and available resource metrics. Missing metrics are explicit nulls with reasons.
- [x] **NFR-09 - Maintainability** Acceptance: Versioned schemas, testable Python functions, separated science/site settings, and a P0 acceptance matrix.
- [x] **NFR-10 - Usability** Acceptance: Prepared test completes within five minutes; tutorial completes within 30 minutes after prerequisites.
- [x] **NFR-11 - Storage efficiency** Acceptance: Temporary preprocessing arrays; capped previews; recorded work/output sizes; documented resume-related duplication.
- [x] **NFR-12 - Restricted privileges** Acceptance: Compute tasks run as the submitting user through Apptainer. Elevated lab setup remains confined to disposable infrastructure.
- [ ] **RE-01 - Pin the exact workflow commit, Nextflow release/distribution checksum, Java vendor/build, Python runtime, package versions/hashes, build tools, and tested Apptainer/Slurm versions.** Acceptance: A release-lock validator rejects missing pins, mutable-only tags, and placeholder values. Capture actual runtime versions on every invocation.
- [x] **RE-02 - Pin the Docker base by platform-specific digest and lock Python wheels with hashes. Build the project code into the image.** Acceptance: The build log identifies every downloaded dependency and the resulting `linux/amd64` OCI digest. No runtime `pip install` or host-code override in release mode.
- [x] **RE-03 - Convert the tested OCI artifact to one canonical SIF, archive that file, and verify its SHA-256 before execution.** Acceptance: A modified SIF is rejected. Local and Slurm acceptance execute the identical SIF bytes.
- [x] **RE-04 - Pin input and reference bytes independently of code.** Acceptance: Changing one byte with an unchanged manifest fails validation; a legitimate new data release gets a new lock and analysis fingerprint.
- [x] **RE-05 - Make randomness independent of execution order and batch membership.** Acceptance: Seed each sample from a stable SHA-256 derivation of global seed and sample ID; initialize Python, NumPy, and OpenCV RNGs. Batch sizes 1 and 16 produce the same scientific results.
- [x] **RE-06 - Control arithmetic and serialization behavior.** Acceptance: Explicit dtypes, image decoding flags, border/connectivity conventions, thread counts, locale, row ordering, float formatting, and missing-value rules are tested.
- [x] **RE-07 - Ensure cache identity tracks consumed content.** Acceptance: Use content-aware file caching and explicit value inputs for per-batch science parameters, reference identity, and SIF identity. Mutation tests prove intended invalidation.
- [ ] **RE-08 - Preserve a complete release independently of an image registry.** Acceptance: Fresh reproduction can retrieve the SIF, code, demonstration data, reference, and locks from the archived release records.
- [x] **T-01 - Unit: scientific functions** Acceptance: Known masks, counts, areas, threshold semantics, empty-image handling, calibration, deterministic ordering, and seeds pass.
- [x] **T-02 - Unit: contracts** Acceptance: Parameter ranges, unknown keys, schemas, missing values, filename handling, manifests, checksums, and output inventories pass positive and negative cases.
- [x] **T-03 - Local end-to-end** Acceptance: Execute all four workflow processes in Apptainer on 12 images; verify outputs, schemas, provenance links, and goldens.
- [x] **T-04 - Clean repeat** Acceptance: Execute twice in independent work/output directories using the same SIF and inputs; satisfy Section 14 comparisons.
- [x] **T-05 - Resume** Acceptance: Interrupt after at least two batch completions; resume the explicit session; verify successful batches were cached and aggregation is complete.
- [x] **T-06 - Invalidation** Acceptance: Change one valid sample plus its data manifest, one scientific parameter, a reference value, and the SIF in separate cases. Verify affected task execution. A corrupted input with an unchanged lock must fail.
- [x] **T-07 - Cache damage** Acceptance: Remove a cached declared output, then resume. Its batch executes again; no incomplete output is accepted.
- [x] **T-08 - Failures** Acceptance: Exercise task code 75, deterministic application failure, missing output, and finalization failure. Validate retry counts, nonzero run status, diagnostics, and absence of a success marker. Candidate-5 full suite passed all cases, including status-commit regression tests.
- [x] **T-09 - Slurm integration** Acceptance: Use 10 batches/array size 4; observe full and partial arrays, actual CPU/memory/time requests, accounting, success, and cleanup. Confirm no child job calls `sbatch`.
- [x] **T-10 - Slurm recovery** Acceptance: Induce a time-limit failure and an accounting-visible OOM where cgroups support it; resume after correction. Observe single-task retry for the separate exit-75 case. No orphan jobs remain after handled failure.
- [x] **T-11 - Portability/offline** Acceptance: Run the same release SIF locally and on Slurm with external network unavailable during execution; compare scientific results.
- [ ] **T-12 - Scale/resource** Acceptance: Run the benchmark protocol, maximum-size image fixture, large aggregation fixture, and 10,000-record planning test. Label each evidence level correctly. Envelope fixtures and 256-image execution passed, but the qualified benchmark protocol was correctly rejected before submission on the shared undersized host.
- [ ] **T-13 - Release/docs** Acceptance: Validate citation/schema/license metadata, archive completeness, public DOI downloads, tutorial commands, and the one-command reproduction path.
- [x] **M-01 - Scientific reproducibility, same tested environment** Acceptance: Scientific masks, integer counts, IDs, and canonical scientific tables/JSON match exactly across clean runs using identical SIF/input/reference bytes. Operational provenance and rendered reports are excluded.; T-04 comparison JSON and checksums.
- [x] **M-02 - Scientific reproducibility, tested local vs Slurm environments** Acceptance: Decoded masks, counts, IDs, and categorical QC match exactly. Finite floating measurements satisfy `abs(a-b) <= 1e-8 + 1e-6*abs(expected)` in the column's declared units; null positions match.; Per-column maximum differences and environment identities from T-11.
- [x] **M-03 - Correctness against original/reference routine** Acceptance: All defined goldens pass, including threshold boundary and empty-object fixtures. No scientific mismatch is hidden by rounding or loosening tolerances.; T-01, T-03; review record.
- [ ] **M-04 - Four-worker strong scaling** Acceptance: `S_4 >= 2.8`, equivalently `E_4 >= 0.70`, on the qualified benchmark.; Raw timings, fixed manifest, resource settings, hardware description, speedup/efficiency chart.
- [ ] **M-05 - End-to-end benefit** Acceptance: Full workflow with four workers is faster than with one on the benchmark; report actual speedup and the serial validation/aggregation fraction.; Stage and whole-run walltimes; no substitution of analysis-only speedup.
- [ ] **M-06 - Workflow/container overhead** Acceptance: One-worker analysis stage at most 1.25× the direct serial containerized routine on the identical dataset and output contract. Investigate batching if the target fails.; Both timing distributions, excluding downloads but including their respective output writes.
- [ ] **M-07 - Resource efficiency** Acceptance: Analysis CPU efficiency median at least 70%; peak RSS at or below 80% of requested memory for at least 95% of benchmark tasks; no OOM/time-limit failures on valid benchmark runs.; CPU efficiency = `TotalCPU/(Elapsed*AllocCPUS)` for matching accounting scope; normalize units; retain raw data.
- [ ] **M-08 - Honest memory sizing** Acceptance: After tuning, median peak RSS should use at least 20% of requested memory, or the resource report explains a site minimum or measured worst-case requirement.; Typical and maximum-size fixture RSS, chosen request, safety margin.
- [x] **M-09 - Resume effectiveness** Acceptance: Every completed batch whose cache entry and declared outputs remain intact is reused; failed/missing batches execute; no duplicated samples.; T-05–T-07 task-state comparison. Report recovered compute time separately.
- [x] **M-10 - Thread/submission discipline** Acceptance: No task exceeds its configured thread budget; actual requests match site config; demo never exceeds four running worker tasks. Normal handled failure cleanup completes within 60 seconds on the lab.; Process/thread inspection, generated job scripts, expanded Slurm job records.
- [x] **M-11 - Provenance completeness** Acceptance: 100% of scientific artifacts trace to an input/reference and software identity; 100% of executed tasks have requested resources and success/failure state. Unavailable usage metrics are explicit.; Schema and relationship validation.
- [x] **M-12 - Python test coverage** Acceptance: At least 85% statement and 75% branch coverage of project Python; every error class and P0 story has a behavioral test/review, including workflow integration.; Current candidate-7 frozen-source evidence: 90.5% statements / 83.2% branches, 169 tests with no skips or failures (`evidence/cycle-11-tests.xml`, `evidence/coverage-cycle-11.xml`). Candidate 5 recorded 90.0% / 82.4% over 157 tests. Python coverage does not measure DSL2.
- [x] **M-13 - Time to first result** Acceptance: Prepared local test run completes within 5 minutes; tutorial within 30 minutes after prerequisites.; Timed fresh-user or recorded self-test, labeled appropriately.
- [ ] **M-14 - Publication completeness** Acceptance: Software and demonstration-data version DOIs resolve; public downloads pass checksums and reproduction; release metadata has no fake/missing identifiers.; T-13 public-release verification record.

## Additional normative deliverables

- [ ] A-01: all CLI commands, exit codes, strict configuration precedence, paths with spaces, and doctor/preflight checks verified.
- [x] A-02: published schemas/data dictionary, immutable versioned demo/reference, canonical identities, artifact relations, and public JSON-LD.
- [x] A-03: pinned candidate OCI/SIF and exact runtime/dependency versions; no fabricated release hashes or DOI.
- [x] A-04: real Slurm lab accounting, 4/4/2 arrays, fixed resources, single-element retries, cancellation, OOM/time-limit drills.
- [ ] A-05: candidate build CI, local Apptainer workflow CI, trusted release checks; actual hosted CI execution recorded when access permits.
- [x] A-06: offline tutorial, architecture/DAG, operations, benchmark, contribution, installation, troubleshooting, and DMS alignment documentation.
- [ ] A-07: documentation/data license texts, third-party notices, valid citation metadata, actual software/data version DOI publication and clean public reproduction.

## Optional P1 backlog

- [ ] Open OnDemand site template with tested CLI equivalence.
- [ ] Second scheduler with real integration evidence.
- [ ] Validated RO-Crate export.
- [ ] Heterogeneous resource classes / automatic resource growth.
- [ ] Eight-worker and physical multinode scaling.
- [ ] SIF signatures and SBOM.

## Verification log

- Initial: `git status --short`, complete file inventory, ancestor instruction search, PRD read, Docker version and WSL inventory.
- Candidate 5 full Linux check: `157 passed`, no failures/skips, 1360.56 s; 90.0% statement / 82.4% branch coverage (`evidence/cycle-9-tests.xml`, `evidence/coverage-cycle-9.xml`).
- Final project checks: Ruff check and format check passed; Actionlint 1.7.12 passed all workflows; requirements/schema audit reported 74 numbered requirements and 58 verified; Python wheel and sdist build passed; 141 host unit tests passed in 78.22 s; 24 local Markdown links resolved.
- Candidate 5 offline local/Slurm acceptance passed with exact SIF SHA and all six floating comparison maxima 0.0 (`evidence/offline-candidate-5/`). Candidate 5 256-image Slurm envelope passed with 16 batches, 256 processed images and 2012 objects (`evidence/scale-256-candidate-5/`).
- Qualified benchmark invocation rejected before trial submission because this shared Docker host does not meet the PRD's >=8 CPU / >=16 GiB idle-allocation gate (`evidence/benchmark-qualification-rejection.json`).

## Blockers and remaining issues

- Public Zenodo publication will require account access and confirmed creator metadata; no DOI or release lock will be fabricated.
- Independent domain and usability reviewers have not been supplied; synthetic tests and author walkthroughs will be labeled.

## Next work / continuation

1. Candidate 7 local work is complete and all current source/evidence checks are green. If runtime code changes, rebuild a new uniquely named SIF, rerun the full check, and preserve its source/SIF identities; do not overwrite historical candidate evidence. Do not edit repository sources, docs, or TASKS.md while the full check runs: integration tests hash the source tree.
2. Candidate 7 is staged at `/scratch/reprohpc-candidate-7.sif`, SHA-256 `0f5297bce295b8a6980d109504f1b556396c5fc35b5e708cff9c865d467b15ac`, OCI `sha256:4a236659ef81e36deb49ba4c191c5427b7196499ee7d6aa854876bdbc8136b86`. Build metadata/log are `evidence/candidate-7.json` and `evidence/candidate-7-build.log`; baked source comparison is in `evidence/report-acceptance-candidate-7/evidence.json`. Offline local/Slurm and 256-image envelope evidence was last collected on candidate 5; rerun `scripts/offline_acceptance.py` on candidate 7 before citing those gates for this candidate.
3. Remaining release work is external: obtain a dedicated >=8-CPU/16-GiB/100-GiB idle allocation for the three-repeat 1/2/4-worker benchmark; enable hosted GitHub workflow execution and record run URLs/durations; confirm creators, affiliations/ORCIDs and redistribution rights; complete bundled FFmpeg/Ubuntu corresponding-source review; publish software and demonstration-data version DOIs; freeze a clean source commit/tag and run fresh unauthenticated `reproduce` on local and Slurm.
4. GitHub CLI authentication exists but its current OAuth scopes omit `workflow`; no remote push or CI dispatch has been attempted. No source commit/tag, Zenodo record, DOI, or public release has been created by this implementation session. Do not claim release completion until T-13/M-14/A-07 are evidenced.

### Cycle 1 - scientific core and contracts

- Implemented deterministic demo-cv-v1, linear-time component measurements, schema validation, immutable manifests/reference, 12 fixtures and independent scalar-convolution/flood-fill goldens.
- Verified: 34 unit tests passed in 12.19 seconds; ruff passed after formatting. Python source/wheel build initiated and audited separately.
- Partial requirements remain unchecked until full workflow/Slurm acceptance.
- Environment limitation: Docker currently exposes 12 CPUs and about 7.60 GiB RAM, below the PRD 16 GiB benchmark provisioning target. No global Docker settings were changed.
- Downloaded and verified real Nextflow 25.10.4 distribution and Apptainer 1.5.3 Debian package; SHA-256 values recorded in environment.lock.json.

### Cycle 2 - workflow and runtime integration

- Implemented the four DSL2 modules, CLI, task provenance, verifier, deterministic export, real runtime/container build scripts, and initial CI workflows. These remain subject to integration gates.
- Verified 58 unit tests, Ruff, and a Python wheel/source build. Built actual Ubuntu-based analysis OCI and canonical Apptainer SIF (199331840 bytes). Linux doctor confirms Nextflow 25.10.4, Java 21, Apptainer 1.5.3.
- Resolved Slurm startup cgroup topology inside the private disposable container: move its processes to a bootstrap leaf, then delegate memory/CPU controllers. A real job ran as researcher UID 1001, with a 128 MiB cgroup limit and accounting MaxRSS 3576 KiB, COMPLETED 0:0. Slurm emits a cleanup warning moving its exiting step daemon to a populated parent; investigate residual processes before declaring cleanup verified.
- Real local Nextflow integration tests are running; no end-to-end or Slurm-array requirement has been marked complete yet.
- Host preflight now reads metadata and PNG headers only; full image hashing/decoding is scheduled on the selected executor. Resume commands retain resolved science and execution overrides.

### Cycle 3 - real engine and scheduler behavior

- Real local run /scratch/local-integration-3/test_real_workflow_repeat_resu0/first completed all four DSL2 processes, passed finalization/integrity and independent goldens. SIF SHA-256: 303d822bbaa6bf9c6977664f11e7fbcab21a6d6d23bf90248955aa119ec9db95. Session 22225921-6f69-430f-8110-e9acfbf1d713. Full repetition/resume suite still running.
- Fixed actual publication failure: Nextflow deduplicates overlapping parent/child outputs before applying publication filters. Analysis now declares each scientific file plus task metadata, publishes only scientific files, and passes the channel-derived batch directory downstream. Aggregation publishes its single complete directory. No work-directory symlinks are published.
- Added artifact-to-task links, cached-origin records, public task provenance, copied schemas, and terminal failure handling. Twenty-nine focused provenance/contract tests passed.
- Real Slurm arrays executed on the same SIF. Found and fixed accounting join: JobID retains array notation; JobIDRaw is a different numeric allocation ID, now retained separately.
- Real Slurm OOM enforcement test passed (512 MiB allocation attempt in a 128 MiB job recorded OUT_OF_MEMORY). Timeout and ten-batch 4/4/2 acceptance still running.
- Added tutorial/installation/configuration/operations/testing/contribution/release/benchmark/acceptance docs and actual CC BY 4.0 / CC0 license texts. Author walkthrough and release remain incomplete.
- Continue from current tests before rebuilding a final candidate; current SIF predates later provenance/launcher-only edits and must not be called the final release image.

### Cycle 4 - acceptance evidence retained

- Local repeat/resume/export test PASSED on actual Nextflow 25.10.4 + the recorded SIF. Includes 12-image independent goldens, clean exact repetition, full intact-cache reuse and verified export roundtrip. Newly added cache-damage test was not part of this already-running test collection and remains unverified.
- Real Slurm suite: 3 PASSED in 293.33 s. Both OOM and time-limit enforcement recorded by accounting. Ten analysis batches produced exactly groups [2,4,4], actual one-CPU/512-MiB/10-minute requests, batch MaxRSS records, and no child sbatch. Raw evidence copied to evidence/slurm/.
- Unit suite: 83 PASSED in 111.37 s; focused negative preflight and HTTPS transfer-protocol tests added. Protocol response doubles are unit evidence only, never DOI/public-download evidence.
- Real SIF maximum-image probe: 4096x4096, 4.724 s, 448412 KiB peak RSS; baked package path /opt/reprohpc/src/reprohpc/__init__.py. Evidence in evidence/resource-max/. Million-row aggregation still running.
- Checked requirements above refer only to these proven behaviors. Publication, comprehensive failure/resume/invalidation, qualified scaling, full coverage and hosted CI remain open. Original routine was not supplied, so G-01/M-03 refer solely to the explicitly characterized synthetic demo reference; no domain endorsement is implied.

### Cycle 5 - resource and failure verification

- Local parameter invalidation PASSED; both original local integration tests completed in 704.74 s. Coverage from that invocation is invalid because concurrent Windows and Linux sessions shared the default coverage database; no threshold claim is based on it. scripts/check.sh now uses a separate Linux coverage file.
- Real retry-policy tests: 2 PASSED in 261.24 s. Exit 75 retried exactly once (attempts 1/2, exits 75/0); exit 23 failed once without retry. Raw trace/status evidence will be retained from /scratch/retry-integration.
- Million-row streaming aggregation passed in 46.196 s with 51596 KiB peak RSS. Actual 256-image Slurm run completed successfully, session 809af3e1-f6af-49ac-b078-76dac18665d0; raw evidence in evidence/scale-256/. This is execution-envelope evidence, not a qualified strong-scaling benchmark.
- Implemented randomized real Slurm benchmark harness, direct serial baseline, resource accounting, and Matplotlib chart generation. Tested summary arithmetic/chart rendering only with explicitly labeled unit timing fixtures. Full scaling gates remain blocked by non-dedicated, <16 GiB host; no speedup is claimed.
- Rebuilding candidate with pip/setuptools/wheel pins and latest runtime metadata; application bytes captured before subsequent non-scientific docs/benchmark edits. Current active build session is tracked by the agent.


### Cycle 6 - 2026-09-13: expanded tests and repairs

- Candidate 2 was built and staged separately as `/scratch/reprohpc-candidate-2.sif`, SHA-256 `b9ead52a94ac06d8e2fb9443d3ca3a76932f1a12e4aaa84c76c548504998aae0`. It remains an unpublished development artifact.
- Expanded real Linux check: **93 passed, 2 failed in 1236.47 seconds**. Full input/reference/SIF invalidation passed, as did arrays, retry, enforced OOM/timeout and deterministic failure cases. Failures were a live-trace observer race (empty header during append) and a real missing-mask cache reuse bug. Raw JUnit is retained as `evidence/cycle-6-before-fixes.xml`. Coverage from this run is invalid because source files changed while the long-running process was loaded; do not use it for M-12.
- Repaired the observer by reading a snapshot of complete trace lines. Replaced the broad analysis output glob with the exact per-sample expected file list, so missing outputs must invalidate their batch. Added a separate corrupt-mask finalization drill; missing masks must now fail at the engine boundary. Reverification is pending.
- Offline candidate-2 local execution passed. Offline Slurm failed hostname resolution after Docker removed its generated hostname entry. Added a persistent loopback `--add-host` mapping to lab commands, and retained controller/worker loopback addresses. A fresh lab with `--init` is being built; network restoration succeeded after the failed drill.
- Removed NumPy/OpenCV imports from the host launcher. Added bounded standard-library PNG header and bool NPY readers, with real NumPy-generated C/Fortran and NPY v1/v2/v3 comparisons and corruption tests. Added hashed `requirements-host.lock`. **104 unit tests passed in 74.36 seconds** before subsequent lineage/release validation additions.
- Provenance now checks relationships between archived manifest, reference/calibration, scientific specification, per-task inputs/parameters/SIF and producing runs; aggregate rows must agree with per-sample rows. Added negative relationship tests. The release schema requires exact runtime/build/dependency pins and rejects placeholder identities.
- Added actual workflow OOM/TIMEOUT then corrected-resource resume tests, plus hard-kill/manual-cleanup origin tracking. These are pending execution, not complete.
- Next: finish candidate 3 and fresh lab, run the full current suite without concurrent source edits, resolve failures, collect exact candidate evidence, then offline execution and remaining release/CI audit.


### Cycle 7 - 2026-09-13 (continued into 2026-09-14 UTC)

- Rebuilt the disposable lab and preserved the previous container, stopped, as `reprohpc-lab-cycle6`. The new lab has an init reaper, private cgroups, a persistent loopback hostname alias, and real Slurm/accounting. No other project's containers were stopped or changed.
- Candidate 3 SHA-256 is `8f9e404fd010152b4728d0ee00f1343779fd37673126ae90ddee85dafe1aa265`, OCI digest `sha256:0d85e7c520d13514cbc1264396c0a7e0fb07d9c77158e3ed920341b8579106d4`. Canonical bytes are staged at `/scratch/reprohpc-candidate-3.sif`; inventory is `evidence/candidate-3.json`. Subsequent host calibration-copy preservation is recorded in source; this remains an unpublished candidate, not a frozen release.
- Offline local AND real Slurm runs passed using identical candidate-3 bytes and an actual host-only virtualenv without NumPy/OpenCV. Every compared floating column had maximum absolute difference **0.0**; masks/counts/QC matched. Evidence: `evidence/offline.json` and `evidence/offline-candidate-3/`.
- Decision D-03: use a uniquely named Docker `--internal` network for offline acceptance. Removing all non-loopback interfaces made Slurm address resolution fail even with a hosts alias. The successful drill had no default route and both external TCP probes failed with `Network is unreachable`; Docker's internal-network isolation blocks other networks. Original networking was restored and the temporary network removed. This is real external-network isolation, not a mocked download layer.
- The full Linux check finished with **127 passed, 1 failed in 1070.36 s**. The failed timeout drill was caused by Nextflow's default early USR2 signal: the task terminated before Slurm reached its hard time limit. The test now requests a harmless CONT warning, retains the hard TIMEOUT assertion, and gives the same injected task three minutes on resume. Focused real Slurm reverification **passed in 264.68 s**; raw records are in `evidence/timeout-recovery-2/`. Production signal behavior is unchanged.
- All other collected tests passed: real cache repair (one batch executed, eleven cached), content/reference/SIF invalidation, handled interruption, hard-kill recovery, real OOM followed by corrected-resource resume, native 4/4/2 arrays, and scalar retry of one failed array element. Frozen-source coverage was **87.7% statements / 78.5% branches**, independently accepted by `scripts/check_coverage.py`. Preserve `evidence/cycle-7-before-timeout-fix.xml`, `evidence/coverage-cycle-7.xml`, and 150 raw files under `evidence/cycle-7-raw/`; this was not an all-green suite.
- Fourteen additional release-gate tests passed separately (0.95 s), covering incomplete pins, tampered evidence, failed/skipped tests, insufficient coverage, and unqualified benchmarks. Two citation/documentation tests also passed. Both additions were outside that full-suite collection and must be included in final checks. Actionlint 1.7.12 passed all three workflows (`evidence/actionlint.txt`); shellcheck/pyflakes were unavailable and explicitly disabled for that check.
- Release CI now accepts the real external lock and acceptance bundle by HTTPS URL/checksum. Evidence cannot be committed into the same source commit that it identifies. The gate reads hashed JUnit, coverage and benchmark outputs plus raw scheduler records; passing handwritten booleans alone is insufficient.

### Cycle 8 - 2026-09-14: provenance audit and candidate refresh

- Fixed a cached-accounting hazard: resumed tasks must never query old native IDs, which Slurm can reuse. Executed jobs persist captured accounting alongside their cache origin; resumed jobs reuse it with `origin_run` scope. Missing original accounting is explicitly identified rather than inferred from a possibly unrelated job. Six new unit cases verify retained records, mixed current/cached queries, and unavailable accounting. A real Slurm resume assertion is included in the next full suite.
- Completed the draft 1.0.0 metadata contract with explicit schema versions for samples, resolved parameters and per-image metrics, and a validated dataset summary schema. CSV column contracts remain unchanged. Added checks relating measurement parameter hashes to their producing task. These are pre-release contract completions; no published data contract has been silently changed.
- Public exports now carry `dictionary/1.0.0.jsonld`, units, checksummed distributions, and explicit input/reference/software/result relationships. Relative term identifiers resolve within the actual package. The archived source now includes PRD.md and TASKS.md so its README links are complete. Unit export tests verify term fragments, distribution hashes and graph relationships.
- Intermediate unit check: **135 passed in 56.78 s**, before the last metadata/lineage additions. The focused provenance/task suite then passed **20 tests in 41.25 s**. Candidate 4 is building; the next full suite must verify the final source without concurrent runtime edits.
- License inventory for candidate 3 contains 12 Python package records and 179 bundled notice files (`evidence/licenses-candidate-3.json`). This is captured evidence, not a substitute for the final attribution/compatibility review. The CFF schema and its CC BY 4.0 license are vendored with verified upstream hashes for offline citation validation.
- Candidate 4 build exited zero. Its OCI digest is `sha256:6e663b8d2b0379ab6da9723cf38edccf401893d04e920dee368a2b46e1a2ebe1`; its SIF SHA-256 is recorded above and was checked again after staging. Lint, formatting, schema/checklist audit, wheel and source distribution builds passed before the current **152-test** collection. No final coverage claim is made until that collection finishes.
- A real host-only CLI walkthrough passed in **612.44 s**: doctor, first run, verify, golden comparison, threshold-200 run, expected comparison exit 5, execution of the exact printed resume command, and verified export/extraction. First result took **65.13 s**. All resumed analysis tasks were CACHED. Raw evidence is in `evidence/tutorial-candidate-4/`; the reviewable exported report is `artifacts/tutorial-candidate-4/report/index.html`. This is a script-assisted author self-test, run concurrently with integration testing, not an independent novice study or a qualified performance benchmark. Direct inspection confirmed blank-image null/NO_OBJECTS behavior and square area 252 pixels = 63 square micrometers at 0.5 micrometers/pixel.
- Derived historical observations are in `evidence/cycle-7-derived-metrics.json`: intact resume reused twelve batches (17.767 original task-wall seconds in aggregate); cache repair reused eleven and executed one; handled interruption reused both completed batches and cleaned up in **2.3775 s**. These sums describe reused original task times, not current elapsed-time speedup. The ten-batch Slurm trace observed at most two concurrent analysis allocations, one thread per task, and UID 1001; the lab's four-worker upper bound was respected.
- Candidate-4 license harvesting is retained in `evidence/licenses-candidate-4.json`. The broad filename filter includes notice candidates; refine any false-positive filenames during the final license review. Publication still requires creator/rights confirmation and component-source redistribution review, including bundled FFmpeg and Ubuntu packages; this inventory is not legal approval.


### Cycle 8 verification results

- The full frozen-source Linux check passed: **152 tests, zero failures/skips, 2040.07 seconds**. Statement coverage **89.7%**, branch coverage **82.4%**; Ruff, formatting, schema/checklist audit, wheel and source distribution builds passed. Evidence: `evidence/cycle-8-tests.xml`, `evidence/coverage-cycle-8.xml`, `evidence/cycle-8-raw/`. Python coverage does not measure DSL2; the sixteen actual integrations separately exercise its behavior.
- Candidate-4 offline local and Slurm acceptance both passed in the host-only environment. The lab had no default route and both external TCP probes failed. All floating-column maximum differences were **0.0**, and masks/counts/IDs/QC matched. Network state was restored. Evidence: `evidence/offline-candidate-4/evidence.json` and its raw run records.
- Real Slurm resource job **58** completed with exit 0 at one CPU / 2 GiB / ten minutes. A 4096-square image took **1.5713 s**, process high-water RSS **449532 KiB**; streaming 1,000,000 rows took **20.9654 s**, **52984 KiB**. Slurm's sampled MaxRSS missed the short image peak; process `ru_maxrss` is retained and explicitly distinguished. Evidence: `evidence/resources-candidate-4.json`, `evidence/resource-candidate-4-sacct.tsv`. These are envelope fixtures, not strong-scaling results.
- Newly checked recovery, provenance, usability, and build requirements are grounded in the full-cycle raw traces, schemas/relationship validation, exact-candidate offline comparison, timed tutorial, and actual OCI/SIF build records. The maximum image/aggregation cases and prior 256-image run support the declared envelope; qualified performance and public publication remain unchecked.

### Cycle 9 - final audit repairs

- Final audit found that success was written before the final integrity check. `finalize_run` now checks the complete package with canonical terminal metadata held in memory, then writes the run record and atomically commits status **last**. The manifest already hashes those final bytes. Decision D-04: treat status as the commit marker; a failed write/crash before that final rename leaves a non-success status. This implements FR-09 without exposing a temporary false success or weakening public verification.
- Candidate-5 frozen-source full check passed: **157 tests, zero failures/skips, 1360.56 s**, with **90.0% statement / 82.4% branch coverage**. Ruff, formatting, schema/checklist audit, wheel and source distribution builds passed. Evidence: `evidence/cycle-9-tests.xml`, `evidence/coverage-cycle-9.xml`, and 1353 selected raw records under `evidence/cycle-9-raw/`. The 22 provenance regression cases include corruption and injected manifest/run/status write failures.
- Candidate-5 offline local and real Slurm acceptance passed using host-only Python with no NumPy/OpenCV, one internal Docker network, no default route, failed external probes, and exact scientific comparison (all six floating-column maxima 0.0). Network was restored. Evidence: `evidence/offline-candidate-5/`.
- Candidate-5 256-image Slurm envelope run passed: 16 batches of 16, 256/256 processed, 2012 objects, verified output, 107.965 s on the shared undersized host. Evidence: `evidence/scale-256-candidate-5/`. This is execution-envelope evidence, not a qualified speedup or resource-efficiency benchmark.
- Candidate-5 license inventory contains 12 Python distributions and 178 installed notices; `evidence/binary-build-candidate-5.json` records the actual OpenCV/FFmpeg build and FFmpeg's reported LGPL 2.1-or-later license. `docs/license-review.md` records the remaining source-preservation and rights review; inventory is not legal approval.
- CI now retains synthetic workflow stdout/stderr/commands/trace/status/accounting on success and failure. The collector was executed against actual cycle-8/9 artifacts. All three workflows cache tool download inputs by immutable environment/dependency hashes; the build helper rechecks downloaded bytes. `container.yml` supports reusable workflow calls. Actionlint passed; cache v4.2.4 SHA was verified against its upstream Git tag. Hosted execution and the 15-minute warm / 25-minute cold duration targets remain unmeasured.
- Documentation link audit checked 24 local Markdown links with no missing targets. Headless rendering inspection confirmed the report's 12 thumbnails, QC, parameters and links are visible; evidence is `evidence/report-visual-candidate-4.json` and `evidence/report-candidate-4.png`.

### Cycles 10-11 - 2026-09-16: review, repairs, and UI completion

- Independent review of the Codex implementation found three real defects; each now has a regression test that fails on the old code and passes on the fix.
  1. The report's "Dataset metadata" link targeted `metadata.jsonld`, which only exports contained; reports opened inside run directories had a broken link. `finalize_run` now writes the JSON-LD metadata and dictionary before the checksum manifest. `reporting.check_report_links` derives every static and per-sample link from the rendered report and requires each target to exist.
  2. Recorded media types came from `mimetypes.guess_type`, which reads platform registries. A Linux-verified package failed `verify_run` on Windows because `.csv` resolved to `application/vnd.ms-excel`. Inventories now use Python's built-in type table; the candidate-7 Linux export verifies on the Windows host.
  3. `ops/slurm/start.sh` aborted after a lab container restart because `sacctmgr add` fails for existing accounting entities under `set -e`. Registration is now idempotent; a restarted lab reached `doctor --profile slurm` ready.
- Candidate 6 (`f08950a6...260a`) passed report acceptance (`evidence/report-acceptance-candidate-6/`) but was superseded by fix 2 before its full check; its aborted check produced no evidence. The earlier unaccepted UI build (`ccc568ec...`) record is `artifacts/candidate-ui-ccc568-superseded.json`.
- Candidate 7 full frozen-source Linux check: **169 passed, 0 failed/skipped, 1686.70 s**, 90.5% statements / 83.2% branches, using real Apptainer and Slurm (`evidence/cycle-11-tests.xml`, `evidence/coverage-cycle-11.xml`, `evidence/cycle-11-check.log`, `evidence/cycle-11-command.sh`). Host: Ruff check/format, requirements audit (74/58), 153 unit tests, wheel/sdist build with report assets, Actionlint on all three workflows.
- Browser suite re-run 20/20 on installed Chrome (`REPROHPC_BROWSER_CHANNEL=chrome`) against freshly generated fixtures, byte-identical to `ui-fixtures-v3`. An earlier invocation in this session without an installed browser replaced `evidence/ui-browser-results.json` with failures; the retained file is the subsequent complete passing run.

### Real-data evaluation - 2026-09-17

- Added an optional real dataset: BBBC039v1 (Broad Bioimage Benchmark Collection, CC0), 200 fields of Hoechst-stained U2OS nuclei with expert masks. `scripts/fetch_bbbc039.py` verifies the upstream archives against pinned SHA-256 values and converts 16-bit TIFF to the 8-bit grayscale PNG input contract by a fixed 12-to-8-bit shift, with no per-image scaling or tuning. The converted dataset and its reference are derived artifacts and are gitignored; the reference declares `pixel_size_um: null`, so `area_um2` stays empty rather than carrying an invented scale.
- Real run through actual Nextflow/Apptainer on candidate 7: 200 images, 13 batches, status success in 87 s, `verify_run` passed over 907 files and all 607 report links resolved. Evidence: `evidence/bbbc039-run.log`, `evidence/bbbc039-trace.tsv`, `evidence/bbbc039-command.sh`.
- Agreement with the expert masks, parameters chosen on the tuning half and reported on the held-out half (`scripts/evaluate_bbbc039.py`, `evidence/bbbc039-evaluation.json`): held-out median count ratio 0.937, median absolute error 5 nuclei, 80.8% of images within 10%, median foreground area ratio 0.914. 98 of 100 held-out images are under-counted and none over-counted, consistent with merging touching nuclei in an algorithm with no splitting step.
- This is real-data engineering evidence, not domain validation. G-01/M-03 remain tied to the synthetic `demo-cv-v1` reference. Count agreement cannot detect a correct count made of wrong objects; instance-level IoU matching and a comparison against a published method (CellProfiler, Cellpose) are the honest next steps. No domain expert has reviewed these outputs. See `docs/real-data.md`.

### Full-screen image viewer - 2026-09-18 (cycle 12)

- Added a full-screen viewer for the selected image in the report's explorer, opened from the detail preview or its Full screen action. It is a native modal dialog element, so the browser confines focus and Escape closes it. Previous/Next and the arrow keys move through images that match the current filters and have an embedded preview, stopping at each end; the position reads "N of M embedded previews" because a report can list far more images than the 24 previews it embeds. Closing restores focus to the control that opened it and keeps the last viewed image selected. The viewer reuses the already-embedded preview, so it makes no request and works from file URLs; printing excludes it. Samples beyond the preview cap offer no full-screen control.
- Browser suite extended to **24 tests, all passing** on desktop and mobile Chrome, including an axe WCAG A/AA audit with the dialog open, boundary behaviour, focus restoration, and absence of the control without a preview. One expectation was wrong on first run (sample "square" is 10th of 12, not 11th); the viewer was correct and the test was fixed.
- Candidate 9 is staged at /scratch/reprohpc-candidate-9.sif, SHA-256 dc501aabf7ad753d6311a1091eed379c9925975063bf47aa9ca903bef7fc413b, OCI sha256:e72b57cb6799ab159de48d459c3933e4cd5945979b056781388245c715e34aa5 (evidence/candidate-9.json, evidence/candidate-9-build.log).
- Candidate-9 report acceptance passed: real Nextflow/Apptainer run, exact golden comparison, verified run and export, 43 report links resolving in both, baked source equal to the checkout (evidence/report-acceptance-candidate-9/).
- Candidate-9 full frozen-source Linux check: **169 passed, 0 failed/skipped, 4530.50 s**, 90.5% statements / 83.2% branches (evidence/cycle-12-tests.xml, evidence/coverage-cycle-12.xml, evidence/cycle-12-check.log, evidence/cycle-12-command.sh). The longer wall time than cycle 11 reflects concurrent load on the shared host, not a regression.
- The BBBC039 real-image run was repeated on candidate 9 and its held-out agreement is unchanged (median ratio 0.9367, 80.8% within 10%), confirming the UI change does not touch scientific output. Intermediate candidate 8 (048fae40...8865) passed acceptance but was superseded by a viewer label correction before its full check; no evidence is claimed for it.
- Note: the lab container was removed once and stopped once by events outside this session (Docker restarts). The reprohpc-scratch volume and both images survived, and the idempotent ops/slurm/start.sh brought Slurm back after each restart.

### Second routine: FSL neuroimaging - 2026-09-22 (Stage 1 of TASKS-NEUROIMAGING.md)

- Added `fsl-bet-volumetry-v1`: FSL BET skull-stripping and `fslstats` volumetry over public OpenNeuro T1w MRI, through the existing DSL2 workflow, Apptainer execution, Slurm profiles, provenance and verification. FSL is called through the Python task layer (`python -m reprohpc.task mri`), so task.json keeps the same provenance fields and adds the routine and the FSL identity read from the image.
- Contracts: six new schemas (`mri_params`, `mri_sample`, `mri_metrics`, `mri_summary`, `mri_analysis`, `mri_run`) dispatched by `algorithm`. Every existing schema file is byte-identical; demo-cv-v1 validation, the image report and its 24 browser tests are unchanged. A 3-D brain mask cannot be expressed in the 2-D image contract without misreporting it, so AGGREGATE and REPORT stay the same Nextflow processes while the Python behind them dispatches; `docs/neuroimaging.md` records that deviation from the original task note.
- Container: `containers/Dockerfile.fsl` installs FSL 6.0.7.23 bet2/avwutils only from `containers/fsl-6.0.7.23-linux-64.lock`, an explicit 74-package lock whose 26 release-pinned packages match FSL's own release file exactly and whose other 48 the release leaves unpinned. 617 MB installed against 4.7 GB for a full FSL; SIF 396,185,600 bytes. FSL's licence forbids redistribution with financial return and requires source with any transfer, so no FSL image is ever committed or released (`THIRD_PARTY_NOTICES.md`).
- Data: `scripts/fetch_openneuro.py` downloads ds000001 1.0.0 with `aws s3 sync --no-sign-request`, verifies every file against the size and MD5 in OpenNeuro's published annex key for that snapshot, and records SHA-256, S3 key and S3 object version. With a manifest present it reconstructs the dataset from those exact object versions. Only manifests are committed; the NIfTI files are gitignored.
- Candidate-3 acceptance, SIF `e55ee8cc555fd3df177b4572f866d52879d03853ceefcf7e7c9494e6c31239f4`, baked source equal to the checkout (`evidence/mri-candidate-3/`): two local runs and one Slurm run all printed `Status: success` and passed `verify` (76, 76 and 77 files). Determinism across all five subjects was exact: identical bytes, identical mask voxel hashes, **maximum `brain_volume_mm3` difference 0.0** local-to-local and local-to-Slurm. All three runs match the stored golden exactly. Slurm ran native arrays `52_0`-`52_3` plus job `53`, all COMPLETED `0:0`, every job in `sacct.tsv`.
- Brain volumes (mL): sub-01 1416.0, sub-02 1538.7, sub-03 1278.6, sub-04 1173.7, sub-05 1508.2. `bet_frac` 0.5 was chosen by visual inspection of three mid-slices on sub-01 and sub-02 only, against no ground truth.
- Honest limitations, all in `docs/neuroimaging.md`: skull-base tissue remains inside the mask at every `-f` tested; sub-04's upstream image is already skull-stripped (86.3% exactly-zero voxels against 7.4-9.4%) and no QC flag caught it; the golden is a regression anchor from our own verified run, not an independent oracle like `tests/reference_oracle.py`; determinism across different CPUs is untested. No FreeSurfer, no clinical data, no XNAT/REDCap, no accuracy validation against manual segmentation or another tool.
- Two defects found by real runs and fixed with regression tests: the launcher passed `write_previews` in the science block to every task, which the MRI contract rightly rejected (every MRI_BATCH task failed); and `scripts/build_candidate.py` read a `Descriptor` field that only Docker's containerd image store provides, which had broken hosted CI on the previous push. Separately, hashed manifests (`samples.csv`, `checksums.sha256`, lockfiles) were `text=auto` without `eol`, so a Windows clone would have rewritten them to CRLF and broken their recorded SHA-256; `.gitattributes` now pins LF.
